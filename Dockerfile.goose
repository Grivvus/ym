FROM golang:1.25 AS builder

WORKDIR /app

RUN go install github.com/pressly/goose/v3/cmd/goose@latest

COPY .env ./.env
COPY db/migrations/ ./migrations/

ENTRYPOINT ["goose", "-env=.env", "-dir=/app/migrations/", "up"]
